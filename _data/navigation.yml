# The following files support website features such as menu links to other pages.
# Default theme pages are included but you can add more.

# The `nav_order` key defines the order of the items in the navigation bar.
# The `title` key defines the text displayed in the navigation bar.
# The `url` key defines the URL the navigation item links to.
# The `icon` key defines the icon displayed in the navigation bar.

# List of all pages in the site.
# Use `nav_order` to control the order of the items in the navigation bar.
# Use `title` to control the text displayed in the navigation bar.
# Use `url` to control the URL the navigation item links to.
# Use `icon` to control the icon displayed in the navigation bar.

main:
  - title: "About"
    url: /
  - title: "Publications"
    url: /publications/
